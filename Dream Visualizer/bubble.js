class bubble {
    constructor(pokemonJN, x, y) {
        this.c = 1;
        this.title = pokemonJN.name;
        this.x = x;
        this.y = y;
    }

    display() {
        // Draw the bubble
        fill("#F1DEDE");  // 
        ellipse(this.x, thi.y, 100, 100);

        // Write the text

    }

}